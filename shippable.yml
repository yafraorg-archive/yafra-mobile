language: node_js

node_js:
  - '0.10'

git:
 submodules: false

before_install:
  #- git fetch --all
  #- git reset --hard origin/master
  - pwd
  - ls
  # ubuntu updates and i386 files
  - sudo DEBIAN_FRONTEND=noninteractive apt-get update -qy
  - sudo DEBIAN_FRONTEND=noninteractive apt-get install -qy libstdc++6:i386 lib32z1
  # ubuntu nodejs cordova ionic global install
  - cd ..
  - sudo npm install -g cordova ionic
  - cd -
  # android SDK
  #- wget http://dl.google.com/android/android-sdk_r23.0.2-linux.tgz
  - wget http://dl.google.com/android/android-sdk_r24.3.3-linux.tgz
  - tar -zxf android-sdk_r24.3.3-linux.tgz
  - export ANDROID_HOME=`pwd`/android-sdk-linux
  - export PATH=${PATH}:${ANDROID_HOME}/tools:${ANDROID_HOME}/platform-tools
  - echo "y" | android update sdk -a --filter tools,platform-tools,build-tools-20.0.0,android-19 --no-ui --force

install:
  - cd ionic/
  - sudo npm install
  - cd ..
  - ./admin-install-upgrade.sh

script:
  - ./build-shippable.sh
